# Proxy para o app (nome do serviço no docker-compose: app).
# resolver 127.0.0.11 + proxy_pass com variável: o NGINX resolve no tempo da requisição,
# evitando "host not found in upstream" no startup quando o DNS do Docker ainda não expõe "app".

# Servidor HTTP (configuração padrão - será atualizada automaticamente pelo script generate-nginx-config.sh)
# Se certificados SSL existirem em nginx/ssl/, o script de deploy gerará configuração com HTTPS
server {
    listen 80;
    server_name _;

    resolver 127.0.0.11 valid=10s ipv6=off;
    set $nextjs_upstream http://app:3000;

    # Logs
    access_log /var/log/nginx/access.log;
    error_log /var/log/nginx/error.log;

    # Health check endpoint
    location /health {
        access_log off;
        return 200 "healthy\n";
        add_header Content-Type text/plain;
    }

    # Proxy para Next.js
    location / {
        proxy_pass $nextjs_upstream;
        proxy_http_version 1.1;
        
        # Headers importantes
        proxy_set_header Upgrade $http_upgrade;
        proxy_set_header Connection 'upgrade';
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;
        proxy_set_header X-Forwarded-Host $host;
        proxy_set_header X-Forwarded-Port $server_port;
        
        # Timeouts
        proxy_connect_timeout 60s;
        proxy_send_timeout 60s;
        proxy_read_timeout 60s;
        
        # Cache de conexão
        proxy_cache_bypass $http_upgrade;
    }

    # Otimização para arquivos estáticos
    location ~* \.(jpg|jpeg|png|gif|ico|css|js|svg|woff|woff2|ttf|eot)$ {
        proxy_pass $nextjs_upstream;
        expires 1y;
        add_header Cache-Control "public, immutable";
        access_log off;
    }
}

